import { PublisherStudio } from "@/components/publisher/publisher-studio";

export default function PublisherPage() {
  return (
    <main className="flex min-h-screen flex-col">
      <PublisherStudio />
    </main>
  );
}
